services:
  - type: web
    name: task-management-api
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/task-management-api-1.0.0.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
